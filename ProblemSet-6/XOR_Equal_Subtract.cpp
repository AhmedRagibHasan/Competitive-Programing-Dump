#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin>>t;

    while(t--)
    {
        int n;
        cin>>n;

        vector<int> a(n);

        vector<int> binary;

        for (int i = 0; i < n; i++)
        {
            cin>>a[i];

            while (condition)
            {
                /* code */
            }
            
            
            int quo = a[i];

            int rem = quo % 2;

            binary.push_back(rem);



            quo = quo / 2;
        }



        
    }
    return 0;
}