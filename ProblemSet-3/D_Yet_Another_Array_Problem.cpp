#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;

    while (t--)
    {
        long long int n;
        cin >> n;

        vector<long long int> a(n);

        // vector<long long int> a2;

        for (int i = 0; i < n; i++)
        {
            cin >> a[i];
        }

        // for (int i = 0; i < n; i++)
        // {
        //     cout<<a[i]<<" ";
        // }
        // cout << '\n';

        // a2 = a;

        // sort(a2.begin(),a2.end() );

        // int x = a2.front();

        for (int i = 0; i < n; i++)
        {
            /* code */
        }
        



        

        


        

       



    }

    return 0;
}